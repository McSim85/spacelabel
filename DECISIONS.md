# spacelabel — Decisions Log

> Every locked decision with **rationale**, **confidence**, and **open questions**. Companion to [`DESIGN.md`](./DESIGN.md). Phase 1 (deep research + adversarial verification) produced these; later phases append/revise and must update the "Affected phases" notes at the bottom.
>
> Confidence scale: **high** = verified on the reference machine or against ≥2 independent authoritative sources; **medium** = single strong source or consistent inference; **low** = inferred, unverified, deferred.
>
> Method: 7-track parallel research workflow → 2 adversarial verifiers (skeptic-framed, with live on-device recon) → completeness critic. Reference machine: macOS 26.5.1 (build 25F80), arm64, SIP **enabled**, Homebrew Python 3.14.4, pipx 1.11.1, uv 0.11.8.

---

## 0. ⚠️ Baseline correction (load-bearing — read first)

The Shared Baseline says *"ctypes `dlopen` of SkyLight for the private CGS reads."* On-device verification proves this is **imprecise on macOS 26** and it must be read as corrected:

- SkyLight has **no on-disk Mach-O** on Tahoe (shared-cache only; `nm` returns nothing) and exports the four symbols **only under `SLS*`** names — **zero `CGS*` exports**.
- **`CoreGraphics.framework` re-exports** them under the legacy `CGS*` names; that is the framework to bind against.
- A binding written as `CDLL(SkyLight).CGSMainConnectionID` works **today only by accident** of process-global symbol resolution (CoreGraphics is already loaded), and is one point-release away from a silent `AttributeError` if the `CGS` alias is ever dropped.

**Corrected decision:** bind against **CoreGraphics**, resolve each symbol **`CGS`-name-then-`SLS`-name**, raise a logged `CGSUnavailableError` on miss → fall back to the spaces plist. **Confidence: high** (empirically dlopened + symbol-resolved live on the reference machine; `dladdr` confirmed `CGS*`→`SLS*` ownership by SkyLight).
**Affects:** the baseline text carried into Phases 2–6; the Phase-4 `cgs.py` implementation; the Phase-6 probe.

---

## 1. CGS read path (highest risk)

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 1.1 | **Bind the `CGS*` name against CoreGraphics; on a miss, fall back to the `SLS*` name against a separately-loaded SkyLight bundle (per symbol).** **Corrected (2026-06-22, Phase-6 item H):** the fallback now targets a real SkyLight bundle (`objc.loadBundle("SkyLight", …)`, cached) — the earlier code tried the `SLS*` name against the **CoreGraphics** bundle, which on Tahoe exports only `CGS*`, so the fallback was a silent **no-op**. SkyLight is loaded lazily, only if a `CGS*` alias is missing. | See §0. CoreGraphics holds the `CGS*` re-exports; SkyLight exports only `SLS*`. **Verified live on the reference machine:** forcing the `CGS*` names absent, every symbol resolves via its `SLS*` name from SkyLight and `CGSMainConnectionID()` returns a real non-zero connection — so the safety net is genuine, not aspirational (if Apple ever drops the `CGS` alias, the reads survive instead of falling straight through to the plist parser). | high (verified live) |
| 1.2 | **Committed read pattern = PyObjC `objc.loadBundleFunctions` of CoreGraphics**, with `{'retval': {'already_retained': True}}` on the two `Copy` functions; auto-bridged `NSArray`/`NSString`. | Only precedent (`css` 2024, `JARVIS` 2025) that *provably* balances the `+1` retain for a long-lived poller; auto-bridge removes manual CoreFoundation plumbing (fewer silent-bug surfaces). PyObjC already locked → free. Critique explicitly required committing to **one** ownership pattern, not a menu. | medium–high |
| 1.3 | **Fallback read pattern = `ctypes.CDLL(CoreGraphics)` + `objc.objc_object(c_void_p=…)` + deep-convert then exactly one `CFRelease`.** Pin `CGSConnectionID=c_uint32`, `CGSSpaceID=c_uint64`; `restype=c_void_p` for Copy results, `c_uint64` for `CGSManagedDisplayGetCurrentSpace`. | Documented contingency for a PyObjC build where the annotation misbehaves, and for debugging. Widths are AltTab-proven on arm64 (the #1 ABI hazard — connection width diverges across sources: AltTab `UInt32` vs Phoenix `NSUInteger` vs CGSInternal `int`; pin to AltTab). | high (widths) / medium (path) |
| 1.4 | **Label key = per-Space `uuid` string, not `id64`/`ManagedSpaceID`.** | `uuid` is what macOS persists to `com.apple.spaces.plist`, so it is the durable cross-reorder/cross-reboot key; `id64`/`ManagedSpaceID` are session-scoped and reassignable (AltTab uses `id64` only because it needs *within-session* identity, not persistence). | medium |
| 1.5 | **Live current-Space via `CGSManagedDisplayGetCurrentSpace`; map id→uuid through the parsed Spaces list.** Do not trust the dict's `"Current Space"` for liveness. | The dict `"Current Space"` can lag right after a switch; the plist definitely lags (cfprefsd cache). The live call is authoritative. | high |
| 1.6 | **Labelable-Space test = `type == 0` AND no `"TileLayoutManager"` key.** Never hardcode the fullscreen `type` constant. | Sources disagree on the fullscreen value (CGSInternal `1`, Phoenix `4`); WhichSpace detects special Spaces by the **presence** of `TileLayoutManager`. Treat any non-zero `type` / that key as skip. | medium |
| 1.7 | **Handle the `"Display Identifier" == "Main"` sentinel** (remap to primary display UUID); defensively remap any non-UUID-parseable identifier. | When "Displays have separate Spaces" is off, the key is the literal `"Main"`; AltTab/WhichSpace both special-case it. | high |
| 1.8 | **Active display = `CGSCopyActiveMenuBarDisplayIdentifier`; fallback = `NSScreen.mainScreen()` CFUUID.** | That symbol is single-sourced (AltTab only). The fallback covers a future rename/removal so active-display logic never silently targets the wrong screen. | medium |

**Open questions (→ Phase 6):**
- 🔑 Is the per-Space `uuid` **stable across reboot**? This is the #1 project assumption. **Upgraded to ~high confidence without a reboot** (follow-up research, 2026-06-19): the uuid is a persisted CFUUID in the `cfprefsd` `com.apple.spaces` domain (read back at login), macOS keys its own reboot-surviving features on it (`app-bindings` "Assign to Desktop", pre-Sonoma per-Space wallpaper), and `dado3212/spaces-renamer` keys names solely on the uuid string with the author stating they "persist between reboots and … re-ordered or recreated." Decisively, **all** known "scrambled on reboot" reports trace to the volatile integer `id64`/`ManagedSpaceID`/index, never the uuid string. Source code alone can't prove it (WindowServer/SkyLight is closed; reference repos only consume the uuid). Confirm cheaply without a full reboot: (1) read-only — match live `CGSCopyManagedDisplaySpaces` uuid to the on-disk plist `Spaces[].uuid` (not the stale `"Current Space"`); (2) near-definitive — logout/login or `killall -HUP WindowServer` (same teardown/reload path as boot) and diff uuid strings. Only a real reboot additionally covers new-display / NVRAM / OS-upgrade edge cases. Phase 6 still does the literal reboot capture as the final gate. **→ Phase-6 result (2026-06-23, `docs/VERIFICATION.md`): the read-only proxy PASSED — every live `CGSCopyManagedDisplaySpaces` uuid was present in the on-disk `com.apple.spaces.plist`, and the plist fallback reproduced the identical labelable-uuid set. So uuid reboot-stability stands at ~high confidence. The *literal reboot* confirm is the one remaining open item (deferred to a natural restart; capture snippet in VERIFICATION.md). The `killall -HUP WindowServer` proxy was intentionally skipped (it logs the user out).**
- Does `already_retained:True` (vs `already_cfretained:True`) correctly balance the `+1` for this PyObjC build? Confirm by RSS-watch in a tight loop (flat RSS = correct; rising = leak; crash = over-release). **→ Phase-6: CONFIRMED — 4000 tight `enumerate_spaces` reads, RSS flat (no leak, no crash). `already_retained` is correct.**
- The PyObjC↔CFArray bridge round-trip was **not** exercised on 26.5.1 (PyObjC absent from the base interpreter during recon); only `ctypes` symbol resolution was confirmed. The bridge is the actual Phase-6 smoke test. **→ Phase-6: CONFIRMED — `CGSCopyManagedDisplaySpaces` bridges to native `list[dict]` and `enumerate_spaces` returns valid `Space` objects on 26.5.1.**
- `id64` vs `ManagedSpaceID` equivalence; `TileLayoutManager` fullscreen marker — design-only, confirm on hardware. **→ Phase-6: CONFIRMED — both `id64` and `ManagedSpaceID` present on every Space; fullscreen (Chrome) Space correctly skipped (not in `spaces`/pills).**

---

## 2. Module & dependency choices

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 2.1 | **Menu bar: raw `NSStatusItem`; reject `rumps`.** | rumps is a thin wrapper over the same ~15 calls, adds no capability, would seize `NSApplication`/run-loop ownership (collides with our delegate + workspace observer), and is a pipx trap (sdist-only, `requires_dist` null → wouldn't even pull PyObjC). Baseline said "rumps only if it clearly wins" — it does not. | high |
| 2.2 | **HUD + overlay: borderless non-activating `NSPanel`**, `canBecomeKeyWindow/Main → False`, `ignoresMouseEvents`, `CanJoinAllSpaces\|Stationary\|FullScreenAuxiliary`, `orderFrontRegardless`. HUD level `NSScreenSaverWindowLevel` (~101); overlay `NSStatusWindowLevel` (25). | Must float across all Spaces and never steal focus (opposite of AltTab, which captures keys). Verified level ordering and collection-behavior semantics. | high |
| 2.3 | **Wallpaper text via `NSBitmapImageRep` + Core Text; reject Pillow.** | PyObjC is already locked → zero new dep, correct system fonts + Retina scaling. Pillow adds a dep for trivial text-over-image. | high |
| 2.4 | **Preferences: view-based `NSTableView`** (dataSource+delegate, editable label column); reject WebView; NSStackView only as a tiny-list fallback. | Data is naturally tabular/editable (UUID, label, display); native columns/selection/resize scale to N Spaces. | high |
| 2.5 | **CLI: `click` (≥8.1); reject `argparse`.** | The surface is a real nested command tree; click cites argparse's "does not allow proper nesting of commands by design"; click earns its third-party slot (declarative options, `Choice`/`Path`, auto-help, `Context`) under stdlib-first. | high |
| 2.6 | **Logging: stdlib `logging` only.** Library uses `getLogger(__name__)` + `NullHandler`; one `setup_logging(mode=…)` at entry; CLI→stderr (WARNING/INFO/DEBUG), agent→`RotatingFileHandler` under `~/Library/Logs/spacelabel/`; optional feature-detected `os_log` mirror. | Matches the logging HOWTO and the agent-quiet/CLI-verbose requirement; `~/Library/Logs` is Apple-conventional, needs no privileges/SIP change. | high |
| 2.7 | **Activation policy: `NSApplicationActivationPolicyAccessory` set in code** (kept). **Update (2026-06-22, 6.8):** the cask bundle's `Info.plist` now **also** declares `LSUIElement=true`, so the bundle is accessory from launch; the in-code policy stays (belt-and-suspenders, and still covers a dev/`uv run` invocation that has no bundle). | No-Dock-icon agent that can still show windows; runtime-settable since 10.9. Was "no bundled `.app` needed for the pipx path" — superseded: the `.app` is now the shipping path, and a real `Info.plist` is the cleaner home for the accessory declaration. | high |
| 2.8 | **Run loop: `PyObjCTools.AppHelper.runEventLoop()`.** | Installs PyObjC's autorelease-pool + exception handling around the AppKit loop; the documented idiomatic GUI loop (vs `runConsoleEventLoop` for headless). | high |

**Open questions:**
- os_log PyObjC import path on Tahoe unverified → keep optional/feature-detected (low; not load-bearing).
- HUD level 101 not covering system alerts on Tahoe — verify on hardware (medium).
- Label-edit commit semantics (Enter vs focus-loss) — **resolved in Phase 3 (9.6): commit on both, Esc cancels.**

---

## 3. Display topology & generality

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 3.1 | **Discover topology at runtime via `NSScreen.screens()`; never hardcode count/resolution/orientation/scale/arrangement.** | Portability requirement; orientation inferred from `frame` (portrait if h>w), scale from `backingScaleFactor`, arrangement from `frame.origin`. | high |
| 3.2 | **NSScreen↔CGS display id: `NSScreenNumber → CGDirectDisplayID → CGDisplayCreateUUIDFromDisplayID → CFUUIDCreateString` == CGS `"Display Identifier"`.** Reach `CGDisplayCreateUUIDFromDisplayID` via PyObjC `Quartz` (it lives in ColorSync since 10.13, re-exported through the CoreGraphics umbrella). | Verified identical to AltTab `Screens.swift`. This CFString is the join key across NSScreen ↔ Spaces array ↔ active-menubar display. A bare path-dlopen of only CoreGraphics could miss the ColorSync symbol. | high (mapping) / medium (link path) |
| 3.3 | **React to attach/detach via `NSApplicationDidChangeScreenParametersNotification` (default center); fully re-discover; never cache UUIDs/counts.** | `CGDisplayRegisterReconfigurationCallback` silently stopped firing on Tahoe 26.1 — the AppKit notification is the reliable trigger. | high |
| 3.4 | **Plist (`com.apple.spaces.plist`) is a topology/UUID-enumeration fallback only — never a live current-Space source.** | cfprefsd flushes it only on Space create/delete, so `"Current Space"` lags ordinary switches; WhichSpace watches the file's delete event and re-queries CGS live. | high |
| 3.5 | **Treat Apple's documented "up to 16 spaces" as sanity/UI context, not a hardcoded cap.** | Officially documented (Tahoe help page mh14112) but a *soft* Mission Control limit; per-display-vs-total unspecified; subject to change; the portability rule forbids hardcoding counts. Useful only to confirm the prefs table needs no virtualization and as a parse-sanity bound. | high (documented) / medium (semantics) |
| 3.6 | **⚠️ Phase-4 correction: `CGDisplayCreateUUIDFromDisplayID` lives in ColorSync on Tahoe, NOT CoreGraphics/Quartz.** Bind it via `objc.loadBundleFunctions` from **ColorSync** (then ApplicationServices fallback) with `already_cfretained`, exactly like the CGS read path — `displays.display_uuid` (see `platform/displays.py`). | DESIGN §3.2/§4 said "re-exported through the CoreGraphics umbrella"; verified false on 26.5.1 — PyObjC's `Quartz` has no such attribute and `dlsym` finds the symbol only in `ColorSync.framework`/`ApplicationServices`, not `CoreGraphics`. The bound function returns the **same** CFUUID string CGS uses as `"Display Identifier"` (confirmed live: display `874A623F…` matched the active-menubar identifier). | high (verified live) |

**Open questions:**
- `activeSpaceDidChangeNotification` remains identity-less and non-deprecated on every future 26.x — inferred from secondary sources (primary Apple doc was an unreadable JS SPA) (medium).
- That `"Main"` is the *only* non-UUID `"Display Identifier"` value on Tahoe — taken from AltTab/WhichSpace; handle any non-UUID defensively (medium).

---

## 4. Space-change observation & debounce

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 4.1 | **Observe `NSWorkspaceActiveSpaceDidChangeNotification` on `NSWorkspace.sharedWorkspace().notificationCenter()`** (not the default center); re-read the UUID every fire. | The workspace notification posts only on the workspace center and carries no Space identity. | high |
| 4.2 | **Trailing-edge debounce ~200ms; the debounced callback does the off-main CGS read, then marshals the UI update to the main thread.** | Rapid switching is common; without coalescing the agent thrashes the CGS path and UI. CGS reads are pure IPC and run off-main (AltTab does this); UI is main-only. | high (need) / medium (exact timing) |
| 4.3 | **Hybrid: events + a 1 s liveness poll, split by cost.** `_poll_reload` watches two change signals: (a) **create/delete** via the **spaces-plist mtime** — a cheap `stat()` on every 1 s tick (the plist flushes on create/delete — 3.4), alongside config/labels/displays; (b) **reorder** via a **live CGS topology signature** (`_topology_signature`: ordered `(display_uuid, uuid, id64)` from `enumerate_spaces`) read every ~3 s (`_TOPOLOGY_POLL_EVERY_TICKS`). The signature excludes `is_current` (active-Space changes are the observer's job, and `is_current` is unreliable per-tick — a failed `CGSManagedDisplayGetCurrentSpace` would flip it and fake a change) and includes `id64` (so multiple no-UUID `uuid==""` Spaces on a display stay distinct; an in-memory tick-to-tick value, never a label key — 1.4 unaffected). The baselines are committed only by a successful `_refresh_impl` (the CGS sig only from live-CGS data; the plist mtime always). | A Mission Control **reorder** fires neither `activeSpaceDidChange` nor `didChangeScreenParameters` (active Space unchanged) and is invisible in the plist, so it needs a live CGS read; create/delete *do* flush the plist, so a cheap mtime watch catches them in ~1 s without CGS. **Cost (measured):** `enumerate_spaces` is ~1 ms median / ~6 ms p99 on the reference machine (15 spaces/2 displays) — not the µs the spec assumed — so only that expensive read is decoupled to ~3 s while the cheap stat()s stay at 1 s (keeps `config`/`label` live-reload and create/delete snappy; reorder ≤3 s is imperceptible). **Codex-driven robustness:** **both** change baselines (CGS sig + plist mtime) are committed only by a successful `_refresh_impl`, never at poll-detection time — so a failed render *retries* rather than consuming the signal and leaving the UI wrong indefinitely. (Settled tradeoff after codex oscillated: retry is correct; a *persistent* render failure that keeps retrying — 1 s for create/delete, ~3 s for reorder — is the symptom of a severe bug, bounded by `agent.log` rotation, and strictly better than silently-stale surfaces.) The CGS sig is committed only from `from_cgs` data (never the stale plist fallback, which would spuriously diff); `_refresh_impl` resyncs both baselines so an ordinary refresh doesn't re-fire either watch; the CGS read splits expected errors (CGSUnavailable/Import → DEBUG) from unexpected (→ WARNING once, re-armed on recovery) and never raises out of the timer callback. The CGS read runs on the **main thread** (consistent with the event path); off-main is the deferred 4.2/Phase-6 work — chosen against for now (marginal at ~1 ms vs thread/marshaling/race complexity, hardware-testable only). | high |

---

## 5. Data model (specified in Phase 1; was the critique's top gap)

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 5.1 | **Two JSON files under `~/Library/Application Support/spacelabel/`: `labels.json`, `config.json`, each with a top-level `schema_version`.** JSON (not TOML) per the task spec. | Named Phase-1 deliverable; `schema_version` enables forward-compatible migrations; Application Support is the conventional user-data location. | high (design choice) |
| 5.2 | **`labels.json`: `{ schema_version, labels: { <space_uuid>: { label, last_display?, created_at?, updated_at? } } }`.** Only `label` required. | Nested per-UUID object leaves room for metadata without a schema break; keyed by the stable `uuid` (decision 1.4). | high |
| 5.3 | **`config.json`: `{ schema_version, modes:{menubar,hud,overlay,wallpaper}, per-mode settings, debounce_ms, log_level }`.** | Mode toggles are the spec's stated config; per-mode blocks keep settings discoverable. | high |
| 5.4 | **Atomic writes: temp file in same dir → `fsync` → `os.replace`.** Readers never see a partial file. | `os.replace` is atomic on one filesystem; this is the standard safe-write idiom. | high |
| 5.5 | **Writers (CLI + prefs window) do read-modify-write under an `fcntl.flock` advisory lock, then atomic replace; the agent watches files and reloads on change.** | Two human-paced writers exist (CLI and the agent's prefs UI); advisory lock + atomic replace prevents lost updates and partial reads; file-watch gives live reload without restart (WhichSpace-style delete-event watch). | high (design choice) |
| 5.6 | **Orphaned UUIDs: retain by default; explicit `label prune` + `label clear` for removal.** | Reorders never orphan; if a deleted Space is recreated with the same UUID the label re-binds; and since cross-reboot UUID stability is unverified we must not auto-delete. | high (policy) / depends on 1.4 open question |

---

## 6. Install & runtime (confirmed end-to-end on the reference machine)

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 6.1 | **One package, one `console_scripts` entry point; agent started by `spacelabel agent` (no separate `spacelabeld`).** | Shares the CGS/store layers; pipx exposes the shim at `~/.local/bin/spacelabel`. | high |
| 6.2 | **Deps: pyobjc-core, -Cocoa, -Quartz, -CoreText, click. PyObjC ships `cp314 universal2` wheels → pipx installs with no compiler. SkyLight/CGS is never a PyPI dep.** | Verified live: `pyobjc-core 12.2.1` universal2 wheel downloads; CGS is dlopened at runtime. | high |
| 6.3 | **No `disable-library-validation` entitlement, no SIP-off, no hardened runtime.** **Update (2026-06-22, 6.8/6.9):** the bundle **is now ad-hoc codesigned** (it must be, for the TCC identity) — but ad-hoc is **non-hardened**, so library validation still isn't enforced and the private-framework `loadBundle`/`CDLL` reads keep working SIP-on with no entitlement. (Was "no codesigning"; that referred to the pipx interpreter.) | Apple-signed system frameworks are out of LV scope, and an ad-hoc (non-hardened) signature does not engage LV; `ctypes.CDLL`/`loadBundleFunctions` of a private framework succeeded live under SIP from both the Homebrew interpreter and the signed bundle. A Developer-ID **hardened+notarized** bundle (deferred, 6.9) *would* re-engage LV — to be validated when that lands. | high |
| 6.4 | **LaunchAgent: `LimitLoadToSessionType=Aqua`, `RunAtLoad`, `KeepAlive={SuccessfulExit:false}`, `ProcessType=Interactive`, absolute `$HOME`-templated paths, logs under `~/Library/Logs/spacelabel/` (mkdir before load). Load via `launchctl bootstrap gui/$UID`.** | Verified `launchctl print gui/$UID` → `session = Aqua` (NSStatusItem needs the GUI session; a daemon has none). `KeepAlive` crashed-only so a menu Quit stays stopped. launchd doesn't expand `~`/`$PATH`. launchctl 2.0, not deprecated `load -w`. | high |
| 6.5 | **Run exactly one agent instance at login (only `RunAtLoad`, no second auto-start).** | macOS 26 has a documented `NSStatusItemChangeVisibilityAction` negotiation loop with ControlCenter (BetterDisplay #5314) that two instances aggravate. | high |
| 6.6 | **uv for dev (`uv venv`, `uv pip install -e '.[dev]'`, `uv run`), pipx for distribution; never share an environment.** | Both stdlib-venv from the same interpreter; pipx uses `venv --without-pip`; no conflict. | high |
| 6.7 | **Reverse-DNS id = `dev.mcsim.spacelabel` (personal namespace), kept as a single source-of-truth constant** reused as the LaunchAgent `Label`, the plist filename, and the `os_log` subsystem. | Repo starts under github.com/McSim85; one constant makes a future namespace rename a one-line change. Aligns Phase 1 with the Phase-2 task's `dev.mcsim.spacelabel` (resolves the prior naming conflict). | high |
| 6.8 | **⚠️ Distribution pivot (2026-06-22, reverses pipx-only #30 / §10.3): ship a signed `spacelabel.app` bundle via a Homebrew CASK; pipx is deprecated.** The agent process **is** the bundle (`CFBundleIdentifier=dev.mcsim.spacelabel`, `LSUIElement=true`), built with **py2app** (build-time only — no new runtime dep) and **ad-hoc codesigned inside-out**. `Casks/spacelabel.rb` ships the prebuilt `.app` (release-attached zip); its `binary` stanza exposes the same exe as the CLI on PATH (one bundle, one identity). The LaunchAgent now execs the bundle exe (`install._resolve_install_shim` → `_enclosing_app_exe`; pipx shim kept as a deprecated fallback). | The Phase-6 click-to-switch finding proved a TCC **identity** problem, not a packaging one: on pipx the agent ran as the shared ad-hoc Homebrew `python` app-stub (id `org.python.python`), so Accessibility showed "python3.x", the grant never bound to the agent, and relaunch didn't help (verified: `AXIsProcessTrusted()` False from a fresh process of that exact binary). A source **formula** (`virtualenv_install_with_resources`) would keep that broken shared-python identity; only a code-signed `.app` (its own cdhash + `CFBundleIdentifier`) fixes it — the cask is just how that `.app` ships. **Verified on the reference machine:** the py2app bundle builds self-contained (embeds `Python.framework` + PyObjC + click), ad-hoc-signs as `dev.mcsim.spacelabel`, the CLI runs + reads live CGS, and the agent starts cleanly from the bundle. | high (build + identity verified) / the grant→True + click-to-switch end-to-end is the Phase-6 hardware gate |
| 6.9 | **Ad-hoc signing for now; Developer-ID + notarization deferred.** Ad-hoc needs no Apple Developer account, no secrets, and is fully automatable in CI (free `macos` runner). | Ad-hoc's cdhash changes each build, so the Accessibility grant **drops on cask upgrade/reinstall until re-granted**, and downloads hit Gatekeeper quarantine (right-click→Open / `xattr -dr com.apple.quarantine`). Developer-ID (paid $99/yr program + a Developer-ID cert in CI + `notarytool`/`stapler`) makes the grant durable and Gatekeeper-clean — a follow-on for friction-free public installs, not a blocker for this-machine + early adopters. | high (caveats documented in README/UI.md) |

**Open questions / residual risks:**
- "Process alive" ≠ "icon visible" on Tahoe: System Settings → Menu Bar visibility toggle and the ControlCenter loop can hide the icon. Surface a fallback + document the Settings check (high-impact, design-mitigated).
- Interpreter pinning: `brew upgrade python` minor bump orphans the pipx venv → `pipx reinstall` (and recreate uv `.venv`).
- macOS-26 PyObjC forward-compat is empirical-only (works on 26.5.1; no vendor cert). Keep CGS reads behind try/except + plist fallback.
- A hardened+notarized py2app bundle would re-engage library validation (out of scope for the pipx/personal path).
- **Accessibility (TCC) grant attaches to the Python interpreter, not "spacelabel"** — surfaced live by click-to-switch (§9.5). On the pipx path the agent process is the ad-hoc-signed Homebrew `python3.x` (verified: `Identifier=python3-…`, `Signature=adhoc`, `flags=0x2`, no TeamIdentifier), so the Accessibility prompt/list shows **"python3.x"**, and `brew upgrade python` changes the binary cdhash → the grant silently drops (same fragility as the venv-orphan bullet above; TCC ignores `argv[0]`/process title, so no runtime fix exists). **Cosmetic + robustness only** — click-to-switch works once that entry is enabled. **✅ RESOLVED (2026-06-22 — see 6.8/6.9):** the only real fix — a code-signed `.app` bundle so TCC keys on the app identity (`CFBundleName=spacelabel`, id `dev.mcsim.spacelabel`) — is now the **shipping** distribution path (a Homebrew cask ships the signed bundle; pipx deprecated), no longer deferred. This relaxed 2.7 and 6.3 (both updated below). The remaining ad-hoc caveat is that the grant re-prompts after a cask upgrade (6.9); Developer-ID would make it durable.

---

## 7. Wallpaper mode (experimental)

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 7.1 | **Ship wallpaper mode disabled-by-default, labeled experimental, documented cosmetic/may-revert.** | No per-Space wallpaper API exists; `setDesktopImageURL:forScreen:` is per-NSScreen; on Sonoma+/Tahoe WallpaperAgent owns state, self-reverts, and silently flips "Show on all spaces" off on repeated sets. | high |
| 7.2 | **Render via `NSBitmapImageRep`+Core Text at `frame.size × backingScaleFactor`; write to a stable per-display temp PNG overwritten in place; set per active screen.** | Retina-crisp, no Pillow; WallpaperAgent reads the file async so don't delete-too-early (blank desktop). | high (mechanics) / low (durability — can't be made durable) |
| 7.3 | **Do NOT edit the WallpaperAgent config store / container plists directly.** | Undocumented, fragile across point releases, risks corrupting the user's wallpaper config. | high |
| 7.4 | **Persist the captured original per display so the mode survives an agent restart: its path in `originals.json` (keyed by display UUID) + a byte `original-<uuid>.png` copy as the fallback when that path is gone; recover path→copy→skip. Sweep our own cache files on a 14-day TTL (composites + original copies; never `originals.json` entries or anything outside the cache, never an active connected display's original).** | After a restart the system reports *our own* composite as the current wallpaper, so the live original was undiscoverable and the mode silently stopped (fifth round). Recording path + copy makes it self-heal across restarts *and* a user reorganizing their Pictures folder. Still cosmetic/best-effort; **no per-Space API** and **never edit the WallpaperAgent store** (7.1/7.3 reaffirmed). | high (mechanics) / low (durability — unchanged ceiling) |

---

## 8. Repository layout & packaging (Phase 2)

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 8.1 | **src-layout: package lives in `src/spacelabel/`** with the DESIGN §2 tree (`platform/` + `agent/` subpackages). | PyPA best practice: disambiguates the repo dir from the package, prevents accidental imports of the in-tree source, and works with both editable installs and pipx. Phase 2 owns final layout (DESIGN §2 is "intent, not a binding contract"). | high |
| 8.2 | **Repo name `spacelabel` under github.com/McSim85; MIT © Max Kramarenko. Created private; now **public** (2026-06) with branch protection on `main`.** | Max's call — personal account for now; a namespace move later is a one-constant change (see 6.7). | high |
| 8.3 | **One console entry point confirmed end-to-end:** `spacelabel = "spacelabel.cli:main"`, plus a `python -m spacelabel` alias; `spacelabel agent` lazily imports and calls `agent.app.run_agent`. | Verified locally: editable install, `spacelabel --help`/`--version`, and `agent` dispatch all work (the §6.1 design, now exercised). Heavy imports (PyObjC, store, agent) stay lazy inside command bodies so the CLI never pulls AppKit for `--help`. | high |
| 8.4 | **Tooling = ruff (lint + format) + mypy `strict` + pytest; pre-commit mirrors the CI gates; CI runs on `macos-latest`.** | Engineering-standards requirement (PEP 8/257/484). The PyObjC framework wheels are macOS-only, so a Linux runner cannot even install the package — CI must be macOS. All four gates are green on the scaffold. | high |
| 8.5 | **Exception renamed `CGSUnavailable` → `CGSUnavailableError`** across code + DESIGN.md + DECISIONS.md. | PEP 8 exception naming (ruff `N818`); keeps the locked design and the code in sync. | high |
| 8.6 | **The LaunchAgent invokes `spacelabel agent`** (locked design §8.1/§9.2/6.1), **not** the Phase-2 task's loose wording "`run`". The plist template `packaging/dev.mcsim.spacelabel.plist` uses `__HOME__` tokens that `install.py` substitutes. | Reconciles the task↔DESIGN wording conflict in favour of the locked design (there is no `run` subcommand). | high |

**Notes / residual:**
- `logging_setup` is implemented for **CLI** mode (stderr at WARNING/INFO/DEBUG); the **agent** file sink (`RotatingFileHandler` + optional `os_log` mirror) is a `TODO(phase-4)` and currently falls back to stderr.
- mypy PyObjC per-module overrides are present but "unused" until Phase 4 imports PyObjC; `warn_unused_configs` is intentionally left off until then.
- pre-commit revs pinned via `pre-commit autoupdate` (pre-commit-hooks v6.0.0, ruff v0.15.18, mirrors-mypy v2.1.0); contributors re-run autoupdate as needed.
- `uv.lock` is gitignored (deps are pinned in `pyproject.toml`; the signed-`.app` cask is the distribution path, §6.8). Revisit if reproducible dev pinning is wanted.
- The pre-commit git hook is **installed locally** (`pre-commit install`). The repo is public and **branch protection on `main` is enabled** (required status check: CI). GitHub Discussions is left optional (enable when wanted); the how-to is recorded in `.github/SETTINGS.md`.

---

## 9. User-facing surfaces — CLI & UI (Phase 3)

> Design-only phase. Deliverables: [`docs/CLI.md`](./docs/CLI.md),
> [`docs/UI.md`](./docs/UI.md) + a rendered mockup
> ([`docs/ui-mockup.html`](./docs/ui-mockup.html); hosted artifact linked in
> UI.md). The scaffolded `cli.py` command tree (§8.3) is treated as canonical —
> **no commands renamed**; everything below is additive.

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 9.1 | **Exit-code contract: `0` success, `1` runtime/application error, `2` usage error (click's own codes), `3` reserved for `status` = "agent not running".** **Extended (2026-06-22, Tier 2b / improvements item I):** `status` now reports `{installed, loaded, running, pid, managed}` and detects **any** running agent — the managed LaunchAgent **or** a foreground `spacelabel agent` — via a non-blocking `flock` probe of `agent.lock` (the same lock `app.py` holds; the agent records its pid there). The **exit code is unchanged** (0 when `running`, else 3); `installed`/`loaded` are informational and never change it. So a foreground dev agent now reads `running (foreground)` / exit 0 instead of the misleading "not running". | Verified against the scaffold (`ClickException`→1, `UsageError`→2). `3` (LSB-style) lets `spacelabel status` compose in shell conditionals without conflating "down" with "errored". Keeping 3 ≙ not-running (rather than adding new codes for installed/loaded) preserves the stable contract while enriching the output/`--json`. | high (live-verified: a foreground agent reports running+pid) |
| 9.2 | **stdout = data, stderr = diagnostics/logs.** Default `spaces`/`label list` output is a **space-aligned table with the header on stdout** (revised in Phase 4 from the original TSV-with-header-on-stderr — Max's call after live testing: the ragged TSV read badly in a terminal). **`--json` is the machine-readable channel** for scripts. Logging never writes stdout (enforced by `setup_logging`). | A human-readable aligned table is the better interactive default; padded columns are not cleanly `cut`/`awk`-parseable, so structured consumers use `--json` (which is why `--json` exists on `spaces`/`label list`/`status`/`config get`). Supersedes the Phase-3 TSV default. | high |
| 9.3 | **Additive CLI flags only:** `--json` (`spaces`, `label list`, `status`, bare `config get`), `--dry-run` (`label prune`), `--active-display/--all-displays` (`spaces`), `--no-load` (`install`). **Updated (2026-06-22, Tier 3):** `uninstall` gained `--purge` / `--yes` / `--dry-run` (the `apt remove` vs `apt purge` model — default keeps data, `--purge` deletes spacelabel's data/caches/logs/completion only; non-TTY without `--yes` refuses → exit 2). The reserved `--keep-labels` is now a **deprecated hidden no-op** (emits a stderr deprecation pointing to `--purge`). **Purge safety (codex-driven, Max chose the conservative path):** `--purge` deletes only paths spacelabel **exclusively owns** — the default store dir (wholesale, and only for the default config, using the canonical `data_dir()` not a `--config` symlink's parent), the global Caches/Logs dirs, and the completion scripts. A **custom `--config`** purges those global dirs but **never deletes files in the `--config`'s own directory** (a sibling `labels.json` could be another app's), and purge **refuses while a foreground agent holds `agent.lock`** (so it can't delete the store out from under a live process). | Cover the spec's machine-output + safety needs without touching the locked command tree. `--keep-labels` was always a no-op (data is kept unconditionally), so hiding+deprecating it and adding the real `--purge` is the honest surface. The conservative custom-config rule **supersedes** uninstall-purge.md's "delete the three known JSONs" — never deleting a file in a directory we don't own is the safer default for a destructive op (the lost thoroughness is a documented manual step). The cask `zap trash:` targets the same exclusive paths. | high (design) |
| 9.4 | **Buttons row = ONE `NSStatusItem` + custom CG view.** Pills show first letter(s) of the label, else the Space number; **current marked by alpha (1.0/~0.4), never color**; physical displays split by a thin vertical divider, drawn L→R; scope toggle all-vs-active display. | Re-states 2.1/6.5 for the UI: N items worsen Tahoe ControlCenter hiding + notch overflow. Alpha (not color) keeps per-label color free as a user tag and reads "current" unambiguously with one-per-display. | high |
| 9.5 | **Click-to-switch is opt-in, OFF in v1; display-only by default.** On enable, guide the two one-time steps (Mission Control "Switch to Desktop 1…N" shortcut + Accessibility), then post the **bound** Switch-to-Desktop chord via `CGEventPost` with a **live** UUID→ordinal map. If the shortcut/permission can't be confirmed, **disable the action with a visible reason — never silently no-op.** | Space *switching* is the one operation behind the SIP/Dock wall; making it explicit keeps the default safe and honors the no-silent-except policy at the UI layer. Ordinals shift on reorder → resolve at click time, never cache. **Implemented (backlog `critical-click-to-switch`) and verified live 2026-06-20 — all 14 desktops switch; see the §9.5 implementation note below.** | high (policy) / high (end-to-end switch verified on the reference machine) |
| 9.6 | **Preferences inline-edit commits on BOTH Return and focus-loss; Esc cancels.** Two-level `NSOutlineView` (display→Spaces) per 2.4; color column via `NSColorWell`. | Resolves the §2 open question ("Enter vs focus-loss"). Matches native table-edit expectations; both commit so a user never loses an edit by clicking away. | high |
| 9.7 | **New `config.json` keys** (additive, schema_version stays 1): `menubar.show_buttons_row` (bool, def false), `menubar.buttons_scope` (`all_displays`\|`active_display`, def all), `menubar.pill_label_chars` (1–2, def 1), `menubar.click_to_switch` (bool, def false); `hud.position` (one of the 9 anchors, def `center`), `hud.margin` (int pt, def 24); `overlay.font_size` accepts `"auto"` as well as int; `overlay.corner` accepts any of the **9 anchors** (def `top-right`). | The UI exposes these as menu/prefs toggles and the `mode`/`config` CLI writes them. Defaults keep the quiet title-only behavior; **HUD/overlay placement is user-configurable** (asked for in Phase 3 review); all are forward-compatible additions. | high (design) |
| 9.8 | **New `labels.json` field `color`** (optional hex string) per entry; **UUID remains the sole key.** | Color is a per-label user tag surfaced in pills/overlay/HUD/prefs; informational/forward-compatible like `last_display` (5.2), never part of the key (1.4). | high (design) |
| 9.9 | **HUD/overlay geometry from one runtime formula keyed on the display's SHORT side** `S = min(width_pt, height_pt)`: `hud_font = clamp(round(S·0.05), 18, 64) pt`; `overlay_font` default 15 pt or `auto = clamp(round(S·0.018), 12, 28)`. **A single shared `anchor_origin(visibleFrame, w, h, position, margin)` helper (the 9-position grid) places BOTH panels** — HUD via `hud.position`/`hud.margin`, overlay via `overlay.corner`/`overlay.margin`. Reposition all panels on `didChangeScreenParameters`; AppKit points → Retina is free. | Portability requirement — nothing hardcoded. Keying on the short side gives identical HUD size on the portrait 2160×3840 and the scaled 4K (both S=1080→54 pt) and a sane 48 pt on a 13" laptop; the clamp catches a native-res 4K (S=2160). One anchor helper makes HUD position configurable (Phase-3 review) and keeps HUD/overlay placement consistent. `visibleFrame` clears menu bar/notch/Dock. | high (design) / medium (exact constants — tune in Phase 6) |
| 9.10 | **Per-Space notes (a task queue) keyed by Space `uuid`, stored on the `labels.json` entry as an optional `notes: [{text, done}]` array** (additive; schema_version stays 1; UUID stays the sole key — same precedent as `color`, 9.8). A **notes-only entry** (notes with an empty/absent `label`) is valid — a task list on an unlabeled Space, shown under a `Desktop N` title. **`label clear` keeps any notes** (demotes the entry to notes-only) so clearing a label never discards its task list; an entry left with neither a label nor notes is removed. The **CLI `note` group is the edit surface** (`add`/`list`/`done`/`undone`/`clear`, 1-based indices, bad index → exit 2). **Overlay checkboxes are display-only glyphs** (`☐`/`☑` reflecting `done`), never interactive controls. New config: `overlay.show_notes` (bool, def true), `overlay.note_font_size` (int\|`"auto"`, def auto = a step below the title). | **The original backlog framing — one note *per display* — was wrong** and is corrected here: notes are per-Space and must follow the Space through reorders exactly like the label (1.4), so they live on the same UUID-keyed `labels.json` entry, **not** in the per-display `displays.json`. Reusing the label entry makes the agent's existing 1 s `labels.json` file-watch reflect a `note add` live for free. The overlay panel is click-through / non-activating and **must never steal focus or capture a click** (2.2 / DESIGN §6.3), so a checkbox there can only be a rendered glyph; toggling stays in the CLI and is reflected on the next refresh. Making the overlay's checkbox interactive would require giving the panel mouse focus — contradicts §6.3 — so it is **out of scope**; the Preferences window (a normal activating window) is the right home for an editable surface if ever wanted. `{text, done}` (not a bare string) captures checkbox state even for a pure-bullet render, leaving the door open without a later schema change. | high (design) |

**Open questions / Phase-4 & 6 follow-ups:**
- `CGEventPost` actually switching Spaces under SIP-on + Accessibility — **narrowed** (see the §9.5 implementation note): the building blocks are verified live (symbolic-hotkey grounding, AX binding, event creation, no-silent disable path); only the end-to-end "granted-AX agent posts the chord → WindowServer switches the Space" remains a Phase-6 GUI check, with disable-with-reason as the safe fallback.
- Friendly display-name resolution (display UUID → human name/model) — best-effort; fall back to a short UUID prefix if unavailable.
- Exact HUD `duration_ms`/fade constants and `S·0.05` coefficient are UX taste — tune live in Phase 6.
- HUD `NSScreenSaverWindowLevel` (~101) vs system alerts on Tahoe remains the §2.2 open question (Phase 6).

**§9.5 implementation note (backlog `critical-click-to-switch`, implemented):** the opt-in
pill-click switch path is now real (`platform/switching.py`, wired through
`agent/menubar.py` + `agent/app.py`); the "not implemented yet" warning is removed. Key
decisions, grounded **live on the reference machine** (macOS 26.5.1, build 25F80):
- **Post the *bound* chord, not a hardcoded "Ctrl+N".** We read the actual key code +
  modifier flags from `com.apple.symbolichotkeys` (`AppleSymbolicHotKeys`, via live
  CFPreferences) and post exactly that. The task wording "Ctrl+N" was imprecise: the
  Mission-Control shortcut is **Ctrl + the desktop number** (the letter-N keycode would
  never switch). Symbolic-hotkey **id == 117 + ordinal** (verified: ids 118/119/120 =
  Switch to Desktop 1/2/3); params are `[asciiChar, keyCode, modifierFlags]` (e.g. Desktop 1
  = `[65535, 18 (kVK_ANSI_1), 0x40000 (Control)]`). The plist modifier bits equal the
  `kCGEventFlagMask*` bits, so they pass straight to `CGEventSetFlags`; posted at
  `kCGHIDEventTap` so the WindowServer hotkey dispatch sees genuine input.
- **The shortcuts ship DISABLED by default.** On the reference machine all of 118/119/120
  are `enabled=False` and ids 121+ are absent — so **disable-with-reason is the live default
  state**, not an edge case. The agent surfaces the specific reason ("enable Switch to
  Desktop N in System Settings → Keyboard → … → Mission Control") as a disabled dropdown
  row + a WARNING log, and stops capturing clicks (so the menu stays reachable). Re-opt-in
  (toggling `menubar.click_to_switch` off→on) clears the disable to retry.
- **Accessibility check binds from HIServices by *path*.** `AXIsProcessTrusted` /
  `AXIsProcessTrustedWithOptions` are not wrapped by PyObjC on Tahoe and the
  ApplicationServices/HIServices bundle will not `loadBundle` by identifier (no on-disk
  Mach-O); `objc.loadBundle(bundle_path=…HIServices.framework)` resolves both (verified).
  `AXIsProcessTrustedWithOptions({"AXTrustedCheckOptionPrompt": True})` drives the one-time
  grant dialog on the first failing click; `kAXTrustedCheckOptionPrompt`'s string value is
  used literally (the symbol is unexported).
- **Live UUID→ordinal map, never cached** (`labeling.ordinal_for_uuid`): rebuilt from a fresh
  `cgs.enumerate_spaces` on every click, since ordinals shift on reorder.
- **Hit-testing** uses one shared pure layout walk (`menubar._pill_layout`) for both drawing
  and the click map, so a clicked pixel resolves to the pill drawn there; capture is gated by
  `hitTest_` returning `nil` when disabled (NSView has no `ignoresMouseEvents` — that is an
  NSWindow property), the click-through that keeps display-only pills opening the menu. An
  **unlabelable pill (`uuid=""`, an `include_unlabelable` default Space) is not a switch target**
  — it has no stable key to resolve to a live ordinal — so a click on it opens the menu like a
  click off any pill, never a dead click that consumes the event and does nothing (review P2).
- **✅ Verified live on the reference machine (2026-06-20):** with Accessibility granted to the
  agent's interpreter and the "Switch to Desktop N" shortcuts enabled, a pill click posts the
  bound chord and **the Space actually switches** — for **all 14 desktops**, not just 1–3. This
  resolves three prior Phase-6 unknowns at once: (a) **id contiguity holds past Desktop 3** (ids
  118–131 = Desktop 1–14 once the user enables them; macOS materializes the entries on enable —
  they were simply absent while disabled); (b) **`kCGHIDEventTap` is the correct tap** (the system
  hotkey fires); (c) the **ordinal→Mission-Control numbering lines up** for the labelable Spaces
  (all 14 reach their desktop). The disable-with-reason path was also exercised (both the
  Accessibility-denied and shortcut-not-enabled reasons surfaced).
- **Known behavior — pills do not refresh on a pure *reorder*:** the agent refreshes on
  `activeSpaceDidChange` + `didChangeScreenParameters` (DECISIONS 4.1/3.3); dragging Spaces in
  Mission Control fires neither (the active Space is unchanged), so the pill row stays visually
  stale until the next Space switch. **Click correctness is unaffected** — the pill carries the
  Space UUID and the click resolves UUID→*live* ordinal, so a click on a stale pill still switches
  to the right Space. Surfacing a reorder live (periodic live-enumerate-and-diff in the 1 s poll)
  is a backlog item (`todo/improvements.md`).
- **Residual Phase-6 nicety:** confirm the ordinal mapping on a multi-display layout where the
  first display's lone Space is unlabelable (`uuid=""`, ordinal 1) — it worked here, but the
  general multi-display numbering vs Mission Control is worth a deliberate check.

---

## 10. Release automation (Phase 5 backlog — critical-release-automation)

| # | Decision | Rationale | Confidence |
|---|---|---|---|
| 10.1 | **Release tool: `googleapis/release-please-action` (release-please), not semantic-release.** | Release-please is the clear winner for this repo: native support for Conventional Commits (already the commit standard), the `python` release type updates `pyproject.toml` directly, integrates natively with GitHub Releases, and the action is actively maintained by Google. semantic-release requires more config, a Node runtime, and separate plugins for Python versioning — no upside here. | high |
| 10.2 | **Version source of truth: direct `version = "..."` in `pyproject.toml` `[project]`.** Removed `dynamic = ["version"]` + `[tool.setuptools.dynamic]`. `__init__.__version__` reads it at runtime via `importlib.metadata.version("spacelabel")`. | Release-please's `python` release type bumps `version = "..."` in `[project]` natively; the former `dynamic` setup had no such field and would have been silently skipped. `importlib.metadata` is the stdlib-standard way to expose `__version__` without duplicating the value (resolves the prior dual-source). Always works for editable installs (the standard dev path) and pipx installs. | high |
| 10.3 | ~~**No PyPI for now — distribution is pipx from GitHub Releases.**~~ **Superseded (2026-06-22, 6.8):** distribution is the signed `.app` via a Homebrew **cask**; pipx is deprecated. `publish.yml` still builds + attaches the wheel + sdist (a `build-wheel` job) for dev / `uv pip install` source installs, but the **primary** artifact is `spacelabel-<version>.zip` (the ad-hoc-signed bundle) attached by the `build-app` job, with `update-cask` PR-bumping `Casks/spacelabel.rb`. PyPI stays deferred (still zero-workflow-change to add). | The pipx-first path is what the click-to-switch finding (6.8) invalidated for the *agent*; the wheel remains a fine dev/source artifact, so it is kept, not removed. | high |
| 10.4 | **Build job on `ubuntu-latest`, not `macos-latest`.** | The wheel is `py3-none-any` (pure Python, no C extensions). PyObjC framework wheels are resolved from PyPI at install time. Ubuntu saves macOS CI minutes. | high |
| 10.5 | **In-repo self-tap, but a CASK (`Casks/spacelabel.rb`), not a source formula.** **Updated (2026-06-22, 6.8):** tap via `brew tap McSim85/spacelabel https://github.com/McSim85/spacelabel` then `brew install --cask spacelabel`. The cask ships the prebuilt ad-hoc-signed `.app` (a release-attached zip); `publish.yml`'s `update-cask` job uses `GITHUB_TOKEN` to PR the `version`+`sha256` bump (no extra PAT). The obsolete `virtualenv_install_with_resources` source formula (`packaging/homebrew/spacelabel.rb`) was **removed** — a source formula would rebuild from source and run the agent as the shared Homebrew python, reintroducing the broken TCC identity (6.8). | No extra repo, no PAT, no secrets (ad-hoc sign needs none). A **cask** is required because the fix is shipping a *prebuilt signed bundle*, not building from source. `depends_on macos: :tahoe` + `arch: :arm64` match the verified/ad-hoc build; universal2 + Developer-ID are the 6.9 follow-on. | high |
| 10.6 | **Renovate: weekly schedule, three manager groups (pep621 / github-actions / pre-commit), `pinDigests: true` for GHA, no automerge. ✅ App installed (PR #5 merged).** | Weekly batched PRs limit macOS CI-minutes churn. Three groups keep GHA, Python, and pre-commit PRs separate for targeted review. `pinDigests` lets Renovate handle SHA-pinning of GitHub Actions. No automerge — changes need a glance before landing. | high |

**Open questions:**
- Distribution is a **cask** shipping the signed `.app` (the formula→cask pivot, §6.8), so there are no source-formula python resources to vendor (`brew update-python-resources` is N/A). The cask carries a single `sha256` of the release `.app` zip, filled by the `publish.yml` `update-cask` job.
- The release-please PR uses `GITHUB_TOKEN`; branch protection on `main` is enabled, so Max merges that PR (and the `update-cask` bump PR) manually — the correct flow.

---

## Cross-phase impact (hand-off)

- **Phase 2 (repo layout): ✅ DONE** — see §8. Repo scaffolded (src-layout, stub modules, `pyproject.toml` with the 4 PyObjC parts + click, `spacelabel = "spacelabel.cli:main"`, ruff/mypy-strict/pytest, pre-commit, packaging, GitHub templates + macOS CI). Initial commit pushed to github.com/McSim85/spacelabel. All four gates green.
- **Phase 3 (CLI/UI concepts): ✅ DONE** — see §9. Deliverables written: `docs/CLI.md` (full command spec — exit codes, stdout/stderr contract, flags, examples), `docs/UI.md` (menu-bar/dropdown/prefs/HUD-overlay spec + runtime geometry) and `docs/ui-mockup.html` (to-spec rendered mockup; hosted artifact linked in UI.md). Command tree treated as canonical (no renames; `agent`, not `run`). Tahoe visibility caveats (6.5/§6.1) and experimental-wallpaper framing (§7) baked into the copy.
- **Phase 7 (repo `CLAUDE.md`): ✅ DONE (initial)** — `CLAUDE.md` exists at the repo root: the standing brief loaded each session (core UUID invariant, architecture map, the load-bearing gotchas — §0 CGS-via-CoreGraphics, the notification-center footgun §4, wallpaper §7, switch-wall §9.5, no-hardcoded-topology §3 — conventions, `uv`/pipx commands, testing reality, identity §6.7). It is intentionally tight and links `DESIGN.md`/`DECISIONS.md` as authoritative. **Living doc — must be refreshed after Phase 4** (final commands/modules) **and Phase 6** (verified gotchas).
- **Phase 4 (code + tests): ✅ DONE** — all stubs filled to the DESIGN §2 tree; `cgs.py` uses the committed CoreGraphics CGS→SLS loader raising `CGSUnavailableError`; data model + atomic-`flock`-write store + prune; Phase-3 §9 wiring done (exit codes 0/1/2/3, stdout-data/stderr-diagnostics TSV+`--json`, additive flags, `Label.color`, `menubar.*`/`hud.*`/`overlay.*` config keys, buttons-row custom view, two-level `NSOutlineView` prefs, the shared `geometry.anchor_origin` nine-anchor helper, friendly display names). **Verified live on the reference machine** (read path: symbol resolution, PyObjC↔CFArray bridge, 14 Spaces enumerated with the current one marked, `label set current` end-to-end) and by mocked unit tests + an adversarial-review workflow. Gates green: ruff + ruff-format + **mypy --strict** + **98 pytest**. Deliverables: working modules, `tests/` suite, `docs/TESTING.md`, macOS CI.
  - **Implementation-forced design changes (this phase):**
    1. **`StorePaths` abstraction** (§5): store functions take a `StorePaths` resolved from `--config` (`config_file`, sibling `labels.json`, lock = `<file>.lock`); keeps paths injectable for tests.
    2. **`install.py` builds the plist in code** (`build_launch_agent` → `plistlib.dumps`) rather than reading `packaging/dev.mcsim.spacelabel.plist` at runtime — the packaging file is NOT shipped in the pipx wheel, so it stays the human reference and a unit test (`test_install`) enforces the two never drift. The LaunchAgent program path is pinned to the canonical pipx shim `~/.local/bin/spacelabel` (warns if PATH resolves elsewhere, e.g. a dev `uv` .venv).
    3. **ColorSync display-UUID binding** (new decision 3.6): `CGDisplayCreateUUIDFromDisplayID` is bound from ColorSync, not Quartz/CoreGraphics (corrects DESIGN §3.2/§4).
    4. **`hud.font_size` defaults to `"auto"`** — HUD font computed per active display via the §9.9 formula (reconciles Phase-1 §7.2's literal `42` with Phase-3 §9.9); an int override is honored. HUD shows on the **active (switched-to)** display, not `mainScreen()`.
    5. **Debounced CGS read runs on the AppKit main thread** in the agent callback — the ~200 ms debounce already prevents thrash; the off-main read (DECISIONS 4.2) is deferred to Phase 6 if profiling shows jank.
    6. **Live-reload via a 1.0 s mtime-poll `NSTimer`**, not kqueue/DispatchSource — a robust simplification, functionally equivalent for human-paced CLI edits (DESIGN §7.3).
    7. **mypy `--strict` scope-relaxed for the 7 AppKit-subclass modules** (`agent/*` + `platform/notifications`): `disallow_subclassing_any`/`disallow_untyped_decorators`/`warn_return_any` off + `attr-defined` disabled (PyObjC has no stubs). All pure logic stays fully strict. `warn_unused_configs` left **off** — the `ignore_missing_imports` override list is intentionally broad (lists frameworks not all imported) for forward-compat.
    8. **`displays.discover_topology` skips a screen whose CFUUID cannot be resolved** (logged WARNING, not silent): a display we cannot identify cannot participate in the UUID-keyed join, and `Display.uuid` is the non-optional join key. ColorSync resolution (3.6) makes this essentially never trigger.
  - **Phase-6 flags surfaced (verify on hardware):** `already_retained` (CGS Copy funcs) vs `already_cfretained` (CGDisplayCreateUUIDFromDisplayID) — both work for single reads; the **flat-RSS tight-loop** test is the arbiter (DESIGN §12 item 3). SLS-fallback branch + `NSScreen.mainScreen()` active-display fallback were not exercised live (CGS names resolved first). `TileLayoutManager`/fullscreen-`type` only covered by mock tests (no fullscreen Space was open). os_log mirror returns `None` (binding absent) — the real OSLog path is unverified. HUD level vs system alerts, panel focus/all-Spaces behavior, single-instance lock under the real LaunchAgent, and the 1 s mtime-poll latency are GUI-only.
  - **Live-testing fixes (Phase-4 manual run on the reference machine):**
    - **Preferences rendered blank** — the view-based `NSOutlineView` delegate was named `outlineView:viewForTableColumn:byItem:`; the correct selector is `outlineView:viewForTableColumn:item:`. The wrong name silently drops the outline to cell-based mode (every row blank). Fixed + a regression test asserts the data source responds to the right selector and not the wrong one. Cells now use the canonical reusable `NSTableCellView` pattern.
    - **Trailing `--verbose`/`--debug` on any subcommand** — they were global-only (had to precede the subcommand: `spacelabel --debug spaces`). A custom `click.Command`/`Group` subclass (`cli._Command`/`_Group`) now injects them into every (sub)command, merges them into the shared `AppContext`, re-raises the stderr level, and pops them before the body runs — so `spacelabel spaces --debug` / `spacelabel agent --debug` work in either position (`--config`/`--version` stay leading). docs/CLI.md §2 updated.
    - **Prefs edits commit only on real change** — view-based editable cells can fire `controlTextDidEndEditing` on reload/teardown; the data source now records each cell's shown text and writes only when the value actually changed, so merely viewing the prefs never persists the "Desktop N" placeholder.
    - **Full dropdown menu** (was Preferences/Quit only) — now mirrors docs/UI.md §2.3: "Rename this Space…", the all-Spaces list grouped under per-display headers (current Space checkmarked; unlabeled shows "Desktop N"; the no-UUID second-display Space shown disabled), live mode toggles, then Preferences/Quit. Clicking a labelable Space row renames it (an NSAlert prompt) — since click-to-switch is off in v1 (9.5), rename is the useful row action. Rebuilt each refresh from the live Spaces.
    - **Preferences now also configures params** (was labels only) — added a UUID column and a settings strip: mode checkboxes + HUD-position / overlay-corner popups (the nine anchors), writing config via `store.set_config_value` (agent reloads live). Numeric params (durations/sizes/margins) stay CLI-only. HUD/wallpaper surfaces are now built lazily so a runtime mode-toggle takes effect without a restart.
    - **Ctrl-C stops a foreground agent** — `AppHelper.runEventLoop(installInterrupt=True)` installs a SIGINT handler (the LaunchAgent still stops via the menu Quit).
    - **`config get`/`config set --help` list every valid key** — an epilog generated from `store.CONFIG_SCHEMA` (so it can't drift from the validator).
    - **Display labels (rename displays)** — new feature beyond the original Space-only spec. Custom per-display names are stored in a **separate `displays.json`** (display UUID -> name; a separate file so the labels read-modify-write never has to preserve it) via `store.load_display_labels`/`set_display_label`. `displays.resolved_name(display, overrides)` (custom, else friendly) is used for the menu's per-display headers, the `spaces` DISPLAY column, and the Preferences display rows — which are now **editable** (commit -> `set_display_label`; empty reverts to the friendly name). `StorePaths` gained `displays_file`/`displays_lock`.
    - **Menu-bar pills indicator** — the buttons row (compact per-Space pills) and the title (current Space's full label) are now **mutually exclusive**: enabling `menubar.show_buttons_row` shows the pills and clears the title; the row view installs/removes at runtime (`MenuBarItem.set_show_buttons_row`) so the toggle takes effect without a restart. (Previously the title was always set, so the full name showed even with the row on.)
    - **Color visibility** — a label's `color` (DECISIONS 9.8) now shows as a small swatch on its dropdown menu row (and still tints the buttons-row pills, 9.4); answers "where does the color apply".
    - **`display` CLI group** — `display set <uuid|current> <name>` / `display list [--json]` / `display clear <uuid|current>`, parity with the GUI display renaming; `list` shows live displays (UUID + resolved name + system/custom + current), falling back to stored overrides when topology is unavailable.
    - **Colorized output (TTY-only, `NO_COLOR`-aware)** — CLI log lines on stderr are colored by level (dim/cyan/yellow/red); `spaces`/`label list`/`display list` print a **bold header** and a **green current row** on stdout. `click.echo` strips ANSI when stdout is piped/redirected and the log formatter checks `stderr.isatty()`, so the data channel stays clean for non-terminal consumers (verified: ANSI under a pty, none when piped).
  - **Review-round correctness fixes:**
    - **P1 — `label prune` data-loss guard:** an empty live Space set (CGS returns nothing, or the plist fallback is empty) no longer makes prune delete EVERY label. A real session always has >=1 Space, so an empty set is treated as a failed read and prune refuses (exit 1) before mutating the store — matching the Preferences "Prune orphans" guard.
    - **P2 — CGS loader fallback:** `cgs._load` now accepts a symbol only when it was actually bound into `resolved` (checked via `resolved.get(name)`), not by trusting `objc.loadBundleFunctions`' return (which is `None` even when it skips a missing symbol). A removed `CGS*` alias now correctly falls through to the `SLS*` name instead of storing `None` and failing later with NoneType-not-callable. (`displays._resolve_display_uuid_fn` already used the correct `in resolved` check.)
    - **P2 — menu-bar mode gating:** `_refresh` gates the title/buttons update on `config.modes["menubar"]`; when off it shows a neutral system-symbol icon (`MenuBarItem.set_inactive`) so the Space label stops updating while the status item — and thus the Preferences/Quit menu — stays reachable. Also extended the agent's live-reload mtime poll to watch `displays.json`, so a CLI `display set` reflects in the running agent.
  - **Second review round:**
    - **Corrupt-file data-safety guard (store):** a write read-modify-write now calls `_guard_before_rewrite` first — a present-but-corrupt `labels.json`/`config.json`/`displays.json` is renamed to `<file>.corrupt` (preserving the bytes) before a fresh write, and a transient read error raises `StoreError` (abort). Previously a corrupt read recovered to empty and the next single-key write silently discarded every other entry.
    - **Wallpaper is one toggle (model/store/agent):** removed the redundant `wallpaper.enabled_experimental` config key (and `WallpaperConfig`); `modes.wallpaper` is now the sole gate, uniform with the other three modes. Before, `mode wallpaper --on` / the menu/prefs toggle set `modes.wallpaper` but `enabled_experimental` stayed false, so the agent never actually rendered wallpaper (the CLI still said "wallpaper: on"). The experimental nature stays communicated via the caveat warning + menu/prefs labels, not a hidden second switch. (Supersedes the §7.2 two-key wallpaper config.)
    - **Empty plist fallback = failed read (cli):** `spaces_plist.read_spaces()` recovers its own errors to `[]`, so the old `except OSError` fallback branch was dead and `spaces` exited 0 with an empty table when CGS was unavailable. `_read_spaces_with_fallback` now treats an empty fallback (after a CGS failure) as both-paths-failed -> exit 1 (CLI.md contract).
    - **Headless-safe tests:** removed a unit test that constructed a real `NSStatusItem` (the menu-bar status bar needs an Aqua session; it aborted the process on a headless/CI macOS runner). The agent UI is covered by import-smoke + the verify workflow + manual runs instead.
  - **Third review round (wallpaper + overlay polish, from live testing):**
    - **Wallpaper composites onto the real desktop image** (was a blank black background). `WallpaperRenderer` captures the current wallpaper as a per-display *base*, draws the label (with a translucent backdrop for legibility) onto a COPY, and writes it to a managed cache (`~/Library/Caches/spacelabel/wallpaper/`, one PNG/display, overwritten in place). The user's original wallpaper file is **never modified** (the concern Max raised). To avoid label-on-label it ignores a base whose path is inside our cache and keeps the remembered original; a purge removes stale cache PNGs (only our `display-*.png`, never originals) to bound disk use. Restart-robustness (persisting the captured original) is a Phase-5 backlog item.
    - **Wallpaper label position** is configurable via the new `wallpaper.position` (the nine anchors, default `center`), placed with the shared `geometry.anchor_origin` in pixel space; reuses the same anchor grid as HUD/overlay so display size/attach-detach are handled uniformly.
    - **Overlay font:** `overlay.bold` (bool, default true) draws the corner label (its title) bold; size stays `overlay.font_size` (int|auto). A per-screen free-text **note/TODO** body on the overlay was deferred to the **Phase-5 backlog** (per Max).
  - **Fourth review round:**
    - **P1 — nil CGS Spaces = failed read:** `enumerate_spaces` now raises `CGSUnavailableError` when `CGSCopyManagedDisplaySpaces` resolves but returns a non-list (nil/garbage), instead of returning `[]`. Previously a nil result looked like a successful empty topology, so `spaces` exited 0 with nothing and never tried the plist fallback.
    - **P2 — `--active-display` never widens:** if the active display can't be resolved (CGS error or empty), `spaces --active-display` now fails (exit 1) instead of logging and returning ALL displays — which would mislead `--json` scripts (especially when the main read fell back to the plist).
    - **P2 — wallpaper renders on the active display:** `_update_wallpaper` maps the active display UUID to its `NSScreen` (same as the HUD) instead of always `NSScreen.mainScreen()`, so on multi-display the active Space's label lands on the right display.
    - **P2 — plist parser skips malformed Space entries:** `parse_spaces_plist` wraps the `type`/`id64` int coercions in `try/except` -> log + skip, so a non-numeric value no longer crashes the best-effort fallback (`read_spaces` only guarded `plistlib.load`).
    - **P2 — `click_to_switch` no longer a silent no-op:** the opt-in switching (DECISIONS 9.5) is still unimplemented, but enabling `menubar.click_to_switch` now logs a one-time visible "not implemented yet; pills are display-only" warning (per the no-silent-no-op policy) and the full implementation is a **Phase-5 backlog** item.
    - **Sibling fixes (verify/sweep workflow):** (a) `list_spaces` now raises `CGSUnavailableError` on a nil result too (same class as the `enumerate_spaces` fix). (b) `read_active_space_uuid` returns `None` when the live current-space id is `0` ("no current Space"), so it can't false-match a Space whose `id64` defaulted to 0. (c) **`log_level` is now wired** (was exposed-but-inert, like `click_to_switch`): `setup_logging` gained `agent_level`, and `run_agent` resolves `config.log_level` into the agent's file-sink level (takes effect at agent (re)start; CLI still uses `--verbose`/`--debug`).
  - **Fifth review round:**
    - **Wallpaper never replaces the real wallpaper with black:** when the original desktop image can't be recovered (e.g. after a restart, the current image is still our own cached composite and no original was remembered), `render_and_set` now **skips** (logs why) instead of painting the black fallback and setting it. Persisting the captured original across restarts remains the Phase-5 backlog item.
    - **UUID keys canonicalized on write:** `set_label`/`clear_label`/`set_display_label` normalize the UUID to the canonical CFUUID spelling (uppercase, hyphenated) via `_canonical_uuid`, so a label/display-name written with a lowercase or braced UUID still matches the live CGS lookup and isn't mis-pruned as an orphan. Non-UUID keys are kept verbatim.
    - **`debounce_ms` live-reload:** the `SpaceObserver` captured `debounce_ms` at construction; `_poll_reload` now restarts the observer when `debounce_ms` changes on disk, so `config set debounce_ms …` takes effect without an agent restart.
    - **Ordinals keyed by object identity:** `assign_ordinals` now keys by `id(space)` instead of `uuid`, so multiple unlabelable Spaces (`uuid=""`) on different displays each get a distinct "Desktop N" instead of collapsing onto one (consumers look up `ordinals[id(space)]`).
    - **LaunchAgent requires the pipx shim:** `_resolve_install_shim` now **refuses** (raises `InstallError`, "install via pipx first") when `~/.local/bin/spacelabel` is absent, instead of writing a transient PATH/venv path (`shutil.which`) into the plist — launchd starts the agent without the shell environment, so a transient path makes the login item fragile. `default_shim` was removed.
    - **🔑 Empty-UUID Spaces + the `wsid` marker (new hardware finding → Phase 6):** a display's **single default Space reports an empty `uuid`** in live `CGSCopyManagedDisplaySpaces` and carries an extra **`wsid`** key the 14 real (labelable) Spaces lack (observed on the reference machine: the secondary 4K display's lone Space had `uuid=''`, `id64=1`, `wsid=…`; its `"Current Space"` dict also had `uuid=''`). macOS has not assigned that Space a persistent UUID, so it **cannot be labeled** (no stable key — the whole premise). **Resolution this phase:** `parse_spaces`/`enumerate_spaces` gained `include_unlabelable=`, and the diagnostic `spaces` command now **surfaces** such Spaces (blank UUID shown as `(none)` / `(no UUID)`, `labelable:false` in `--json`) so the display is visible; label/prune/agent paths still operate on labelable Spaces only. **Phase 6 must characterize** when/whether the UUID gets assigned (hypothesis: after adding a 2nd Space on that display via Mission Control) and whether it then survives logout/reboot. The on-disk plist is no help — confirmed **stale** (cfprefsd cache: real-display monitor entries empty), reinforcing DECISIONS 3.4 (plist = enumeration fallback only, never live).
  - **Sixth review round:**
    - **Canonicalization made symmetric (read == write == live):** the write-side `_canonical_uuid` (fifth round) is now the shared `labeling.canonical_uuid`, applied at EVERY UUID boundary — not just writes. **Reads** (`store.load_labels`, `store.load_display_labels`) normalize keys so a legacy/hand-edited `labels.json`/`displays.json` with a lowercase or braced UUID still matches and isn't mis-orphaned. **Live sources** normalize too: `cgs.parse_spaces` (`Space.uuid`), `cgs._normalize_display_identifier` + `cgs.active_display_uuid` (real-UUID branch), `spaces_plist.parse_spaces_plist` (`Space.uuid`), and `displays.display_uuid`. So `stored == loaded == live` holds regardless of what spelling the WindowServer emits or a file holds; non-UUID sentinels (`"Main"`, `""`) still pass through verbatim, preserving the Main-remap and unlabelable-`uuid=""` handling.
    - **Overlay font/bold live-reload:** `overlay.font_size`/`overlay.bold` were captured only in `Overlay.__init__`; `Overlay.set_font(font_size, bold)` (no-op when unchanged) is now called from `_update_overlays` each refresh, so a `config set overlay.font_size`/`overlay.bold` takes effect without an agent restart (the panel re-fits on the following `set_text`). Same stale-config class as the fifth round's `debounce_ms`.
    - **P2 — cosmetic active marker degrades on a PyObjC-less host:** `cli._active_display_uuid_or_none` caught only `CGSUnavailableError`; a `ModuleNotFoundError`/`ImportError` from `active_display_uuid`'s lazy AppKit import would still abort `display list`/`spaces`. It now also catches `ImportError`, honoring the `_or_none` contract — those commands print stored metadata with no active marker instead of crashing.
    - **P1 — "defer PyObjC imports in `agent/app.py`": assessed not actionable.** `app.py` defines `class AppDelegate(NSObject)` at module scope, which forces `objc`/AppKit resolution at import; deferring would mean relocating the ~700-line ObjC subclass into `run_agent` (an anti-pattern, and `tests/test_agent_imports.py` introspects these module-level classes — the established pattern across all `agent/*` modules). The CLI already isolates this via the lazy `from spacelabel.agent.app import run_agent` inside the `agent()` command, so every non-agent CLI command runs PyObjC-free; the project is macOS-only (PyObjC hard dep, CI `macos-latest`, won't build on Linux), so a "PyObjC-less environment" is out of scope by design.
    - **Sibling fixes (verify/sweep workflow):** the workflow confirmed all four fixes above and surfaced same-class siblings, now fixed: **(a)** the `ImportError`-degrade was incomplete — three more CLI legs caught too narrowly and would crash (not degrade) on a PyObjC-less host: `_read_spaces_with_fallback` caught only `CGSUnavailableError` (so an `ImportError` from the lazy `import objc` **skipped the pure-stdlib plist fallback that should run**), and `_display_names` + `display list`'s topology leg caught only `OSError`. All now also catch `ImportError`. The three hard-fail `current` paths (`label set current`, `display set current`, `spaces --active-display`) now catch `ImportError` too — they still fail (a literal `current` needs the live read) but with a clean exit-1 `ClickException` instead of a raw traceback. **(b)** `display clear <literal-uuid>` computed its `existed` pre-check on the **raw** CLI value against canonical stored keys, so a lowercase/braced UUID printed "nothing to clear" right after successfully clearing it; `_resolve_display_target` now canonicalizes the literal branch. **(c)** `spaces_plist.parse_spaces_plist` canonicalizes a **real-UUID `Display Identifier`** too (it only canonicalized `Space.uuid`), mirroring `cgs._normalize_display_identifier`, so the plist-fallback display grouping joins against the canonical live topology. Each fix has a regression test (the prior `display list` degrade test was strengthened — it had mocked `discover_topology` to `[]`, masking the topology-leg gap).
  - **Refresh `CLAUDE.md` (Phase 7) at the end of this phase — done** (architecture map + command list reconciled, ColorSync gotcha added).
- **Phase 5 (backlog): ✅ DONE** — `todo/` folder created at repo root with a `README.md` index + two Critical prompt files + one Non-critical batched prompt:
  - **Critical** (gate a shippable release): `critical-click-to-switch.md` (implement opt-in `CGEventPost` Ctrl+N pill-click switching, v0.2/v0.3), `critical-release-automation.md` (release-please + PyPI OIDC + Homebrew tap + Renovate, v0.2+).
  - **Non-critical** (polish/DX): `improvements.md` covers per-screen overlay note body (v0.2), wallpaper original persistence + per-display font sizing + user-change detection (v0.3), and CLI shell autocomplete (v0.2).
  - Open questions in §1/§2/§3/§6 that require hardware verification remain deferred to Phase 6 (not promoted to backlog prompts). Wallpaper durability ceiling (§7) bounds the wallpaper improvements item in `improvements.md`.
  - **Backlog progress — `critical-click-to-switch`: ✅ DONE.**
  - **Backlog progress — `critical-release-automation`: ✅ DONE.** release-please + pipx-from-release + in-repo Homebrew formula + Renovate implemented; see §10. Key changes: (1) `pyproject.toml` now uses a direct `version =` field (not `dynamic`); `__init__.__version__` uses `importlib.metadata.version()`; (2) no PyPI for now — wheel/sdist attached to GitHub Release; (3) Homebrew formula in this repo (no separate tap repo needed). Renovate app installed (PR #5). Go-public steps documented in `.github/SETTINGS.md`. Opt-in pill-click Space switching implemented end-to-end (`platform/switching.py` + `agent/menubar.py` hit-testing + `agent/app.py` wiring); the "not implemented yet" warning is replaced by the real disable-with-reason path. New pure helper `labeling.ordinal_for_uuid`; new tests `tests/test_switching.py` + `tests/test_menubar_buttons.py` (+ `ordinal_for_uuid` cases). Findings folded into the **§9.5 implementation note** above; the only residual is the Phase-6 end-to-end GUI verification. Gates green (ruff + ruff-format + mypy --strict + pytest). `todo/README.md` row marked done.
  - **Backlog progress — `wallpaper` (restart-robust originals + cache eviction): ✅ DONE.** New decision **§7.4**: the captured original is persisted per display — its path in `originals.json` (keyed **only** by the stable display UUID via `displays.display_uuid` — no transient `CGDirectDisplayID` fallback, which would orphan a capture once the UUID resolves; atomic temp→`replace`) plus a byte `original-<uuid>.png` copy — so `_base_image_path` recovers in order live→remembered-path→copy→skip (the fifth-round "never paint black" skip is preserved, and `_draw_composite` now **raises** rather than black-filling when a base can't be loaded, so a corrupt base never replaces the wallpaper). The **in-memory** recovery map records the live original under **both** the session `CGDirectDisplayID` key and the UUID (when resolvable), and recovery checks both — so within-session recovery survives *either* identifier changing (a UUID-resolution flip **or** a `CGDirectDisplayID` churn; only both at once is unrecoverable), and a UUID-less display still keeps updating all session. Cross-restart recovery reads the UUID-keyed persisted map directly. Only *disk* persistence (and thus cross-restart recovery) needs the stable UUID. User wallpaper changes are caught on Space change (the renderer captures any non-cache current image — `_is_ours` uses component-wise `Path.is_relative_to`, not a string prefix, so a sibling dir sharing the cache's textual prefix isn't misclassified as ours). New `wallpaper.font_size` (int|`"auto"`, default `"auto"`); `"auto"` = `geometry.wallpaper_font_size` (short-side × 0.12, the mode's long-standing prominent size, ×scale at render, with the historical 48 px absolute floor reapplied so 1x displays/projectors stay legible); int honored. The config **loader** rejects `font_size < 1` (falls back to default), matching the `config set` lower bound, so a hand-edited `0`/`-1` can't yield an invisible label (applied to all int|`auto` font fields). `_purge` is now a 14-day TTL sweep over our own `*.png` only (keeps current composites + connected displays' active originals + **any cache PNG still applied as a live screen wallpaper**, so a stale-`CGDirectDisplayID` composite isn't deleted while in use; sweeps **only our own naming** (`display-*`/`main`/`original-*.png`) so a foreign PNG in the dir is never deleted; `originals.json` retained — tiny, maximizes recovery; nothing outside the cache touched), logged at DEBUG. Original copies overwrite via temp→`replace` (a mid-copy failure can't truncate the last good fallback); a re-capture fires on a path change **or** an in-place rewrite (copy older than the source by mtime). Two per-display maps: the live path is always remembered **in memory** (within-session recovery works even if the copy write fails), while the disk path map (`originals.json`) plus the `original-<uuid>.png` copies are written in **one critical section** under a **non-blocking** advisory `flock` (re-reading inside the lock so a peer display's entry is never dropped; non-blocking so the Space-change render path never stalls on a busy peer — it just skips and retries), **copy first, then json**: the copy both captures the bytes and *proves the source is readable inside the lock* (TOCTOU-free — a source that vanished after the caller's `exists()` check makes the copy fail here), so a path whose copy we couldn't capture is **never** committed to `originals.json` (the last known-good handle is kept). A failed json write changes nothing (retried next render); a copy that landed but whose json write then failed is healed on the next render via the in-memory `_copied` provenance (no needless re-copy), and within-session recovery meanwhile uses the in-memory `_originals` entry. Persistence is retried from the **recovery** branch too (not only on a fresh live capture), so a persist skipped at first capture — lock busy or write error — still catches up on subsequent renders even after our composite is the current image (the no-op guard means a successful persist isn't redone). Recovering from the persisted path also **heals** the copy (if a prior run wrote json but not the copy) and **seeds** the recovered base (from the path or the copy) into the in-memory map, so a restart-recovered base stays usable through a later transient UUID-resolution loss. Recovery re-reads `originals.json` from disk when our cached entry for a display is **missing or now dead** (not only when missing), so a peer instance's write or repair of that entry is picked up rather than skipped. **Every original copy still referenced by `originals.json` is kept** regardless of connectivity (never evict the only backup for a display that may reconnect, even after months unplugged — the map is tiny); only truly *unreferenced* orphan copies are TTL-swept, so UUID-resolution health never gates eviction. The copy preserves the source size+mtime and tracks which path its bytes belong to (a separate in-memory map), so an in-place rewrite is detected (a size change or a newer mtime), a copy left stale by an earlier failure is reliably re-copied (not skipped by an mtime coincidence), and a skewed/future-dated source mtime doesn't trigger a needless rewrite every render. The `_is_managed_png` sweep matches our **exact** filenames (`main.png` / `display-<int>.png` / `original-<canonical-UUID>.png`), so a foreign `display-reference.png` / `original-photo.png` in the cache dir is never an eviction target. Residual limit: cross-restart recovery is robust whenever `displays.display_uuid` resolves (the normal case); a session with no resolvable UUID still composites onto the live image and recovers **in-memory** for the rest of the session, but does **not** persist and so can't recover after a restart — an inherent limit (no stable per-display key exists without the UUID, the same key the whole app relies on), and a degraded capture is never misfiled under a transient id. All `codex review` rounds folded in (TTL-vs-live-wallpaper; atomic copy; no transient-id *disk* persistence; UUID-degraded eviction suppressed; two-map memory/disk split; in-memory recovery keyed by both the display id and the UUID (survives either-identifier churn, keeps degraded sessions working); component-wise `_is_ours`; sweep restricted to our own filenames; 48 px auto floor; flock-serialized `originals.json` read-merge-write; json-leads-copy ordering in one critical section so the persisted path is never older than the copy; non-blocking lock + dead-source-path guard so the render path never hangs and never commits an unreadable path; load-side `font_size < 1` rejection; copy preserves source mtime + tracks copied-path provenance; referenced originals never evicted so eviction never drops a reconnectable display's only backup). Touches `agent/wallpaper.py`, `agent/geometry.py` (`wallpaper_font_size`), `model.py`/`store.py` (`wallpaper.font_size` key), `agent/app.py` (passes the configured size), `docs/CLI.md`. New `tests/test_wallpaper.py` + geometry/store cases. Gates green (ruff + ruff-format + mypy --strict + pytest). **Residual: NSImage compositing stays GUI-only — verify on hardware in Phase 6** (restart recovery, user-change refresh, label legibility).
  - **Backlog progress — `space-notes` (per-Space task queue): ✅ DONE.** New decision **§9.10**: per-Space notes keyed by Space `uuid` (corrects the original per-*display* framing), stored on the `labels.json` entry as `notes:[{text,done}]`; notes-only entries valid; `label clear` preserves notes; the CLI `note` group is the edit surface; overlay checkboxes are display-only glyphs (click-through constraint §6.3); new `overlay.show_notes` / `overlay.note_font_size` config. Touches `model.py` (`Note` + `Label.notes`), `store.py` ((de)serialize + `add_note`/`set_note_done`/`clear_note` + notes-only load/prune + config schema), `cli.py` (`note` group; `label list` now omits notes-only entries), `agent/overlay.py` (`set_content` title+list render) + `agent/geometry.py` (`overlay_note_font_size`) + `agent/app.py` wiring. New tests across `test_store.py` / `test_cli.py` / `test_geometry.py` / `test_agent_imports.py`; `docs/CLI.md` §3.7 + `docs/UI.md` §4 updated. Gates green (ruff + ruff-format + mypy --strict + pytest). **Residual: overlay notes rendering is GUI-only — verify on hardware in Phase 6** (title+list layout, auto-resize, glyphs).
    - **Review-driven refinements (this session):**
      1. **Notes-only entries read as UNLABELED everywhere a user sees them:** `spaces` table shows `(unlabeled)` and `spaces --json` emits `label:null` (not `""`); the Preferences color well is disabled (color is per-label, 9.8). Menu/pills already routed through `title_for`/`pill_text` (empty text → `Desktop N`).
      2. **`spaces` gained a `NOTES` column / `notes` count** so a notes-only (or any noted) Space is discoverable in the enumeration, not only via `note list <uuid>`.
      3. **Target is validated as a UUID-or-`current` — but only on the CREATE paths** (`label set` / `note add` / `display set`, via `labeling.is_uuid`): a transposed `note add list current` (or any non-UUID literal) is a usage error (exit 2) instead of silently creating an entry for a Space that can't exist. The **operate/read paths** (`clear`/`done`/`undone`/`note list <key>`/`label clear`/`display clear`) pass `validate=False` so a **pre-existing** legacy non-UUID key stays inspectable and removable (else such entries would be un-cleanable from the CLI — review fix). Existence is **not** enforced — labeling/noting a currently-absent Space by UUID is valid under retain-by-default, 5.6.
      4. **`label clear` demotion drops the per-label `color` and bumps `updated_at`** so a later `label set` never inherits a stale color and the edit is timestamped.
      5. **Overlay panel is clamped to the screen on BOTH axes** (`geometry.overlay_max_content_extent`, margin-reserved so it fits every anchor) and word-wraps; a long note line or long task list can't push it off-screen (overflow clips).
      6. **`label prune` prunes labels, NOT tasks:** an orphan that still has a note queue is **demoted to notes-only** (label+color dropped, tasks kept) rather than deleted, and a notes-only orphan is left untouched — so the label-maintenance command never silently destroys a Space's task list (mirrors `clear_label`). Only orphans with a label-and-no-notes are deleted; `prune_labels` reports the UUIDs whose label was pruned.
      7. **`note list` with no target enumerates all note-bearing entries** (UUID + count) so a notes-only queue is discoverable/recoverable even when its Space is not live (`spaces` lists only live Spaces; `label list` lists only labels). Closes the "no CLI path to find orphan notes" gap.
    - **Schema decision — `schema_version` stays 1** (per the task) for both the new `notes` field and the `overlay.show_notes`/`note_font_size` config keys. The config keys are additive exactly like 9.7's keys (old readers ignore them). The **`notes` field is additive, but notes-only entries (no `label`) are NOT readable by a pre-notes build** — it drops them on load (it requires a non-empty `label`). A version bump would not prevent that loss (the released v0.1.x reads best-effort regardless of `schema_version`) and the constant is shared across labels/config/displays, so bumping is over-broad and ineffective. Mitigation is the standard one: **restart the agent after upgrading** (so no pre-notes process is writing `labels.json` during the skew window). Documented rather than bumped.
  - **Backlog progress — `improvements.md` (Non-critical batch):**
    - **C. CLI shell autocomplete: ✅ DONE (#21).** `completion install` writes click's *generated* completion script into each shell's auto-load dir (fish/bash: no rc edit; zsh: `_spacelabel` into a writable dir on the live `$fpath`, prefers `~/.zfunc`, never plugin/cache dirs; rc fpath-wiring only as a fallback). Dynamic completers for `label`/`note`/`display` targets + `config` keys, all best-effort (never raise into the shell). New `src/spacelabel/completion.py` + `tests/test_completion.py`; `docs/CLI.md` §3.10.
    - **D. Single rotated agent log (merge stderr; one file; fix double-writer): ✅ DONE.** Reframed from the original "rotate `agent.err.log`" (Max's call, per the WIP backlog reorg): **one** managed rotated log, not a second unbounded file. (1) `agent.log` stays owned **solely** by the `RotatingFileHandler` (1 MB × 4). (2) `run_agent` installs `logging_setup.install_logging_excepthook()` — a `sys.excepthook` that logs uncaught exceptions at CRITICAL so tracebacks land in the rotated `agent.log`, not raw stderr (`KeyboardInterrupt` delegates to the default hook). (3) **Double-writer fixed:** launchd `StandardOutPath` **and** `StandardErrorPath` now both point at a single separate **`agent.boot.log`** (was `agent.log`/`agent.err.log`) — so launchd never co-writes the file the handler rotates; this boot-catch file only catches catastrophic pre-logging output and is truncated **in place** by `logging_setup.truncate_boot_log()` once it exceeds 256 KB. Ordering (codex): truncate runs **right after the single-instance lock and before the agent/config setup** — lock-first so a rejected duplicate never truncates the live agent's file, truncate-before-setup so each `KeepAlive` restart bounds the file even if startup then crashes (inherent residual: a crash before `run_agent` runs, e.g. an import error). In-place truncate (never rename): launchd holds it open as fd 1 & 2, so a rename would orphan those fds (codex P1, from the prior copy-truncate round). AGENT logging is configured before the lock so the rejection lands in `agent.log` (the file operators inspect), not the boot file. **Managed-run gating (codex P2/P3 — launchd vs foreground):** the boot-log truncation, the plist refresh, and the startup hard-exit run **only** for the production login agent (`app._is_managed_run`: default config + no `--verbose`/`--debug` + **no controlling TTY**). The TTY check is the launchd-context proxy (a LaunchAgent's std streams are files, a manual shell run is a TTY) — chosen over an env var like `XPC_SERVICE_NAME` because it is **fail-safe**: it can't be verified under real launchd in this environment, and a wrong env-var assumption would silently disable the whole migration for the real agent, whereas a LaunchAgent reliably has no TTY. **Bounded residual:** a *manual* non-TTY run (`nohup`/redirected/CI) also reads as managed, but the harm is bounded — truncate/refresh/`os._exit` run only **after** the single-instance lock, so only the lock winner acts, and a non-TTY run that wins the lock is the de-facto agent (truncate/refresh idempotent/beneficial; `os._exit` just exits non-zero with output already redirected). A *verified* launchd signal (`XPC_SERVICE_NAME`/ppid) is deferred to Phase 6 — it can't be validated against real launchd in this env, and a wrong positive that the real agent must satisfy would be worse than this lock-bounded misclassification. A `--config` run (own store/lock, throwaway config) or a `--debug` foreground run must not zero the real agent's boot log, rewrite the user's installed plist, or die via `os._exit`. On a startup failure the non-managed run **stashes the error + stops the run loop, and `run_agent` re-raises it from a normal frame** — a plain `raise` inside the AppKit callback would be swallowed by PyObjC (same fact behind the `_refresh` guard), so this is what actually yields a terminal traceback / non-zero exit / normal cleanup instead of a wedge. **Upgrade path (codex P1 ×3):** an already-installed plist keeps the old `agent.log`/`agent.err.log` paths until refreshed, so (a) `truncate_boot_log()` also caps the legacy `agent.err.log` (keeps the unbounded-growth fix working pre-migration) and the agent **re-caps the boot log every ~60 s** of the poll (not only at startup, so recurring AppKit/PyObjC stderr can't grow it within a long session), and (b) `install.refresh_plist_if_stale()` — managed run only — rewrites a stale on-disk plist **atomically** (temp→`os.replace`, also adopted by `install_agent`) patching **only** the std-stream paths so `ProgramArguments` (incl. any `--config`/extra args) and other customizations are preserved (never repoints the shim) so the path/single-writer migration applies on the next login without a manual `spacelabel install` (idempotent; no-op when current/absent; malformed plist — `InvalidFileException` *or* expat `ExpatError` — logged not raised). It deliberately does **not** self-`bootout`/`bootstrap` the running login agent (fragile, GUI/launchd-only-testable); the legacy log files stay capped for the current session. The stale plist's `StandardOutPath=agent.log` is left to the `RotatingFileHandler` (which owns/size-bounds `agent.log`; the agent emits no raw stdout) rather than truncated (that would drop the handler's logs); the double-writer ends when the refreshed plist loads. **Recovery semantics (resolved after codex oscillated — PyObjC swallows callback exceptions, the key fact that reconciles it):** because PyObjC eats exceptions at the callback boundary, a steady-state failure neither reaches `sys.excepthook` nor crashes the process, so "let it propagate for a launchd restart" does **not** work — it just vanishes to raw stderr and wedges the agent without a trace in `agent.log`. Final model, split by phase: **startup** (`applicationDidFinishLaunching_`) logs to `agent.log` then **fails fast** (`os._exit(1)`, which PyObjC cannot swallow) → launchd restart + lock release (we want a restart when uninitialized); **steady-state** (`_refresh`, event-driven — the 1 s poll only refreshes on a store mtime change, so failures are event-paced not per-second) is **guarded to log at CRITICAL into `agent.log` and keep the last-good surfaces** (`os._exit` over one transient refresh glitch would risk a restart loop; the next event retries; rotated `agent.log` bounds recurrence). Expected failures stay handled specifically in-line. Top-level (non-callback) crashes route to `agent.log` via `sys.excepthook`. PyObjC 12.2.1 has no global callback hook, so a callback exception outside these guarded paths lands in the bounded `agent.boot.log` — a documented limit, not silent. Touches `logging_setup.py`, `agent/app.py`, `install.py`, `packaging/dev.mcsim.spacelabel.plist` (plist↔template sync test enforces parity), tests in `test_logging.py`/`test_install.py`. **Option B (`newsyslog`) rejected:** on Tahoe `~/Library/newsyslog.d/` does not exist and `/etc/newsyslog.d/` needs root, so a user-level pipx install can't drop a conf. See DESIGN §9.2.
- **Phase 6 (verification):** run the §12 checklist of `DESIGN.md`. **Gate the whole project on item 1 (uuid reboot-stability) and item 3 (RSS-flat memory).** If `uuid` is not reboot-stable, revisit decision 1.4 (the entire UUID-keying premise). **Refresh `CLAUDE.md` (Phase 7)** with any verified gotchas / corrected assumptions from the probe.
- **Phase-6 blockers (2026-06-22, branch `feat/signed-app-cask`): ✅ implemented; on-hardware grant + click-to-switch is the remaining gate.** Cleared the blockers from `todo/phase-6-blockers.md`:
  - **Tier 1 — distribution pivot (6.8/6.9):** signed `spacelabel.app` via Homebrew **cask** replaces pipx. New `packaging/py2app/` (launcher + setup), `tools/build_app.sh` + `tools/codesign_app.sh` (inside-out ad-hoc sign) + `tools/make_icon.py` (placeholder master → `.icns` via `sips`/`iconutil`), `Casks/spacelabel.rb` (`app`+`binary`+`zap`), `install._resolve_install_shim`→`_enclosing_app_exe` (LaunchAgent execs the bundle exe), `publish.yml` `build-app`/`update-cask` jobs, `__init__.__version__` Info.plist fallback in the frozen bundle. Obsolete source formula removed. **Verified headless:** build self-contained + ad-hoc-signed `dev.mcsim.spacelabel`, CLI + agent run from the bundle. **Deferred to Max on hardware:** install the cask → grant Accessibility to the named "spacelabel" entry → confirm `AXIsProcessTrusted()`→True + a pill click switches Spaces (Phase-6 rows B18–B26 / H4–H6).
  - **Tier 2a (1.1):** the CGS→SLS fallback now resolves from a real SkyLight bundle (was a no-op) — live-verified.
  - **Tier 2b (9.1):** richer `status` (install + run state, detects a foreground agent via `agent.lock`) — live-verified.
  - **Tier 3 (9.3):** `uninstall --purge` (+ `--yes`/`--dry-run`, deprecated `--keep-labels`); cask `zap` kept in sync.
  - All gates green (ruff + ruff-format + mypy --strict + pytest); each unit passed the `codex review` loop before commit. **`CLAUDE.md` (Phase 7) refreshed** for the cask distribution.

## Completeness gate (Phase 1)
Critic verdict: **ready-with-open-questions** — design may be locked with the §0 correction baked in (done) and the data model specified (done, §5). No unresolved blockers remain for Phase 2; the load-bearing empirical assumptions (uuid stability, CF memory ownership, the live bridge round-trip) are explicitly deferred to the Phase-6 probe rather than left implicit.
