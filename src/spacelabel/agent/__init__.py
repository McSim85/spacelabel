"""The long-lived menu-bar agent and its UI surfaces (DESIGN.md §6).

:mod:`spacelabel.agent.app` owns the ``NSApplication`` accessory app and run
loop; :mod:`~spacelabel.agent.menubar`, :mod:`~spacelabel.agent.hud`, and
:mod:`~spacelabel.agent.overlay` are the three display modes;
:mod:`~spacelabel.agent.prefs` is the preferences window.
"""
